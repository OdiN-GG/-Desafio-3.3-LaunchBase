{% extends "layout.njk" %}

{% block head %}

    <title> portifolio - Wallison Silva</title>

    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
         rel="stylesheet">

{% endblock %}

{% block content %}

    <div class="apre">
        <h1>Vídeos</h1>
    </div>

    <section class="cards">
        
        {% for item in itens %}
        <div class="card" id="{{item.id}}">

            <div class="card_image_container">
                <img src="https://img.youtube.com/vi/{{item.id}}/maxresdefault.jpg" alt="Servidor Sniper Bf4 (OdiN)">
            </div>

            <div class="card_content">
                 <p>{{item.title}}</p>
            </div>

            <div class="card_info">
                <p>{{item.duration}}</p>
                <p class="card_price">{{item.price}}</p>
            </div>

        </div>
        {% endfor %}
        
    </section>

    <script src="scripts.js"></script>
   
{% endblock %}